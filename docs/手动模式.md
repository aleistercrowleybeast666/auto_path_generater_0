# 手动模式

MANUAL 保存用户定义的唯一 START、任意 WAYPOINT/ARRIVAL、yaw 和全部机械动作。程序严格保持点的位置和顺序，不调用 leg optimizer。

“生成/更新当前路径”执行确定性曲线构造、基于 `z=v²` 的速度参数化、四轮转速与运动学约束、连续碰撞检查、动作编译和最终放货验证。这里“不优化几何”不等于跳过速度规划。

正式比赛 Case 的最后动作必须为 DROP_1/2/3/12/23 的 STOP_AND_WAIT，并绑定最终 ARRIVAL。工作草稿可以暂时不完整，但导出 final 会被守卫拒绝。
