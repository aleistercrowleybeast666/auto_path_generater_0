# 360 批量生成

在“最优路段与批量生成”页点击“全自动生成全部 360”。任务只写 `cases/full_auto`、`bin/full_auto` 和批量报告；已有 MANUAL/SEMI_AUTO 文件不受影响。

系统以 `traj_id.csv` 为唯一映射，复用相同有向 leg，优化缺失/STALE 路段，逐 Case 比较总时间并完整验证。可随时请求取消；未通过验证的 Case 不写 BIN，所有成功输出采用原子替换。
