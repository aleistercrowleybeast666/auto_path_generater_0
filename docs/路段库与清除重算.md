# 路段库与清除重算

`leg_library.json` 保存可复用 directed legs。

清除 leg 只修改 `leg_library.json`，不会修改 `project.json`：

```powershell
python -m hjmb_pathgen.cli clear-leg-result --project <project> --leg-id <LEG_ID> --confirm-leg-id <LEG_ID>
```

批准或锁定的 leg 需要确认 token。清除后必须显式运行优化或生成命令，不会自动重算。
