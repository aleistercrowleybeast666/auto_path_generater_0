# 全自动模式

FULL_AUTO 单条操作只需要 traj_id。系统读取项目公共姿态、物理场地对象、`traj_id.csv` 映射、卸货配置和动作配置，生成任务候选、8 个逻辑点、动作与有向路段需求。

缺失或过期的 unique legs 只在用户明确生成时优化。候选按运动时间、机械时间和 post-wait 的真实总时间比较，选择最快可行方案。第二页可生成当前 ID 或全部 360，并提供进度、取消和报告。

全自动结果默认只读。需要人工拖点、改 yaw 或动作时，应先转为 SEMI_AUTO 副本。
