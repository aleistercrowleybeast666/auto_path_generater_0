# HJMB Path Project Example

This is a synthetic V4.0 example project generated for Phase 9 delivery checks.

Use it to exercise route-table parsing, unique-leg collection, task assembly,
mode layout reports, golden manifest generation, and package smoke tests.

Do not use these coordinates as real competition calibration.
